{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Quiet Companion",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "runtimeArgs": ["."],
      "preLaunchTask": "build",
      "console": "integratedTerminal"
    }
  ]
}
